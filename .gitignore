node_modules/
.env
.env.local
.env.*.local

logs/
*.log
npm-debug.log*

coverage/
.nyc_output/

dist/
build/

.DS_Store
.vscode/
.idea/

prisma/migrations/dev.db*

# Firebase Admin service-account credentials — never commit. Generate
# from Firebase Console → Project Settings → Service Accounts → Generate
# new private key, save the JSON here (or point FIREBASE_SERVICE_ACCOUNT
# env var at a path outside the repo).
firebase-service-account.json

